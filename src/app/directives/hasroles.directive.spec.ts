import { HasrolesDirective } from './hasroles.directive';

describe('HasrolesDirective', () => {
  it('should create an instance', () => {
    const directive = new HasrolesDirective();
    expect(directive).toBeTruthy();
  });
});
